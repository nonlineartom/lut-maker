# Branding assets

Drop `icon.png` (square, 512–1024 px recommended) in this folder and the
app will pick it up automatically as both the favicon and the brand mark
in the header. Anything else you put here can be referenced by path
(`assets/yourfile.ext`).

If no `icon.png` is present the app falls back to an inline SVG mark.
